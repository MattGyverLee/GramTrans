# Cycle 6 -- Live Verification Checks (read-only)

All opens were `writeEnabled=False`. Nothing was written; no project was modified.

## Measurement 1 -- CmAgent stock-template default identity

**CONFIRMED-LIVE.**

Opened three unrelated read-only projects fresh (`Ejagham Mini`, `Esperanto`,
`Mbugwe LizzieHC practice`) via `flexicon.FLExProject.Agents.GetAll()` and read
GUID / Name / Human for every `CmAgent`. All three carry exactly **4 agents**,
and the GUIDs, names, and Human flags are byte-for-byte identical across all
three projects:

| GUID | Name | Human |
|---|---|---|
| `1257a971-fcef-4f06-a5e2-c289de5aaf72` | M3Parser | False |
| `5093d7d7-4f18-4aad-8c86-88389476df15` | HCParser | False |
| `67e9b8bf-c312-458e-89c3-6e9326e48aa0` | Computer | False |
| `9303883a-ad5c-4ccf-97a5-4add391f8dcb` | default user | True |

Cycle-5's assertion holds: default CmAgents are stock-template furniture with a
corpus-wide-stable GUID, not per-project-minted identity. They must not be
counted as transferred/created objects under a natural-key identity rule.

## Measurement 2 -- Yi Sichuan corpus-selection figures

**CONFIRMED-LIVE.**

`Yi Sichuan` is present on this machine
(`C:\ProgramData\SIL\FieldWorks\Projects\Yi Sichuan`, 89 MB `.fwdata`). Opened
fresh, read-only, and walked `ICmObjectRepository.AllInstances()` counting by
`ClassName`:

- `ReversalIndex`: **7**
- `ReversalIndexEntry`: **25,116**

Both figures match cycle 5's citation exactly, both from a fresh live read
(not just re-reading the cached prescan JSON, though that cache independently
agrees: `scratchpad/prescan_results/Yi Sichuan.json`).

## Measurement 3 -- Three-axis maxima from the prescan cache

**PARTIAL: class-presence axis CONFIRMED-LIVE (via cache); WS-breadth and
structural-depth axes NOT-AVAILABLE-ON-THIS-MACHINE.**

Read all 84 valid result JSONs in `scratchpad/prescan_results/*.json`
(`_enumeration.json` excluded -- it is the run manifest, not a result).

- **Class presence (`n_classes`)**: measured maximum is **120 classes**, a
  three-way tie across `Tlachichilco Tepehua-Speedtest`,
  `Tlachichilco Tepehua-NT orthography`, and `Tlachichilco Tepehua-NT Noparse`
  (runner-up: `Ngoreme` / `Ngoreme Johnny` at 115).
- **Writing-system breadth (`writing_systems.total`)**: every one of the 84
  result files has `writing_systems: null`. Not measurable from this cache.
- **Structural depth (`nesting_depth`)**: every one of the 84 result files has
  `nesting_depth: null`. Not measurable from this cache.

Root cause, confirmed via git history: `debug/prescan_type_coverage.py` picked
up the `writing_systems`/`nesting_depth` instrumentation in commit `a841b0e`
("PRESCAN EXTENSION -- captures the two dimensions..."), timestamped
2026-08-18 00:24:07+03:00 (~21:24 UTC). Every file under
`scratchpad/prescan_results/` was last written between ~20:51 and ~21:04 UTC
the same UTC day -- 20-30 minutes **before** that commit landed. The cache on
disk was generated by the prior script revision (`53b8465`), which only
emitted class counts. **The corpus has not been re-swept with the current
producer**, so the two orthogonal axes cycle 5/6 wants committed to a tracked
file do not exist in measurable form anywhere on this machine yet. A rerun of
`debug/prescan_type_coverage.py` (read-only, ~85 projects) is required before
those two maxima can be reported; no substitute or estimate is offered here.

## Summary table

| # | Measurement | Verdict |
|---|---|---|
| 1 | CmAgent default identity is corpus-wide-stable | CONFIRMED-LIVE |
| 2 | Yi Sichuan: 7 ReversalIndex / 25,116 ReversalIndexEntry | CONFIRMED-LIVE |
| 3a | Class-presence max = 120 (3-way tie, Tlachichilco Tepehua variants) | CONFIRMED (cache) |
| 3b | WS-breadth max | NOT-AVAILABLE-ON-THIS-MACHINE (cache stale, needs rerun) |
| 3c | Structural-depth max | NOT-AVAILABLE-ON-THIS-MACHINE (cache stale, needs rerun) |
