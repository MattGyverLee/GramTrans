# Cycle 2 Verification -- 038 transfer-fidelity-gaps

**Verdict: PASS** (all 7 checks pass; one minor naming nuance noted under #6)

1. **PASS** -- `git diff --quiet -- specs/038-transfer-fidelity-gaps/probes/t124/owner-probe-GT038-T124-Ngoreme.json` exits 0 on `main`; file reads `"FsFeatStruc": {"subtree_total": 120, ...}`, the pinned value (not the 1679 crash-residue). Cmd: `cd GramTrans && git diff --quiet -- specs/.../owner-probe-GT038-T124-Ngoreme.json; echo $?` -> `0`.
2. **PASS** -- `specs/038-transfer-fidelity-gaps/probes/t123b/` exists on `main`, containing `owner-probe-GT038-T123b-Ngoreme.json` (`FsFeatStruc.subtree_total: 1679`) and `README.md`. Cmd: `grep -n subtree_total probes/t123b/owner-probe-GT038-T123b-Ngoreme.json` -> first hit `1679`.
3. **PASS** -- `cycle1-verification-gate.md` exists under MAIN's `specs/038-transfer-fidelity-gaps/reviews/`; `find` over the WORKTREE's `specs/` tree for the same filename returns nothing.
4. **PASS** -- Two commits above `009642e` on the worktree branch: `db41744` touches exactly `src/gramtrans/Lib/categories.py`, `tests/unit/test_038_t123_lexreftype_key_and_create.py`, `tests/integration/_snapshots/census-038-t123b-ngoreme.json`, `tests/integration/_snapshots/recensus-038-t123b-ngoreme.json` (4 files, per `git show --stat`); `1edb442` touches `debug/run038_t124_recensus.py` plus `tests/unit/test_038_t124_probe_tag_isolation.py`.
5. **PASS** -- `python -m pytest tests/unit -q` at worktree HEAD (`1edb442`): `3881 passed, 79 skipped, 14 xfailed in 22.59s`. No `tests/integration` or bare `pytest tests/` run, per hazard note.
6. **PASS** (with a naming nuance) -- Static read of `debug/run038_t124_recensus.py`: `_probe_out_dir()` (line 130) now returns `_MAIN/"probes"/RUN_TAG`, computed at call time (not a module-level constant), and both `_probe_path()` (owner-probe, line 421) and the T124-supplements write (line 604) go through it, so a `--tag t123b` run lands in `probes/t123b/`, not `probes/t124/`. The recensus summary filename is also tag-derived: `recensus-038-%s-%s.json % (RUN_TAG, pair)` (line 746). Nuance: the supplements file's *basename* is still the literal string `t124-supplements-<source>.json` regardless of tag -- only its parent directory is tag-derived -- so the leak is closed (nothing writes into `probes/t124/` under a different tag) but the basename text itself does not encode the tag. Default `t124` behaviour is confirmed byte-for-byte unchanged: docstring states "When `RUN_TAG == "t124"` (the default) this resolves to the original hardcoded path unchanged," and `RUN_TAG` still defaults to `"t124"` (line 112).
7. **PASS** -- `git status --short` on `main` still shows `M .specify/extensions/companion/commands/speckit.companion.resume.md`, uncommitted; not present in either new worktree commit's stat output.

## Notes
- `probes/t124/` on `main` is unrelated to the `.crew` doc-agent's out-of-scope paths (`tasks.md`, `journal/`, `STATUS.md`); no overlap observed.
- Two untracked review files (`cycle2-programmer-refile.md`, `cycle2-programmer-t123-commit.md`) present in MAIN's `specs/038-transfer-fidelity-gaps/reviews/` at time of check -- out of scope for this checklist, not evaluated.
