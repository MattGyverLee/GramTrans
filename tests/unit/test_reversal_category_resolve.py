"""Unit tests for reversal-category (`PartOfSpeechRA`) resolution against
the per-index `PartsOfSpeechOA` list (`Lib/reversals.py`).

Scaffold only (tasks.md T003) -- User Story 2 tests (T021-T024: target-list
binding, CREATE path, diverged dispositions, absent-list + caching) land in
a later spurt.
"""
from __future__ import annotations

import pytest


@pytest.mark.skip(reason="scaffold — US1/US2/US3 tests land later")
def test_scaffold_placeholder():
    """Placeholder so this file collects cleanly before US2 tests land."""
    assert True
