"""tests/verification -- offline dev/CI harnesses (not runtime unit tests).

Distinct from ``tests/unit`` and ``tests/integration``: modules here are
verification tooling (e.g. the model-driven fidelity census, FR-011) run as
a dev/CI harness against a live project pair, not a per-transfer runtime
gate. See ``specs/024-lexicon-reference-fidelity/contracts/fidelity-census.md``.
"""
